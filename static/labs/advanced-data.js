(()=>{'use strict';
const $=id=>document.getElementById(id),canvas=$('glcanvas'),gl=canvas.getContext('webgl2',{antialias:true});
const ui={memory:$('memory'),flow:$('flow'),layout:$('layout'),reserve:$('reserve'),writePos:$('writePos'),writeNormal:$('writeNormal'),writeUV:$('writeUV'),copy:$('copy'),useA:$('useA'),useB:$('useB'),modeStat:$('modeStat'),updateStat:$('updateStat'),copyStat:$('copyStat'),code:$('code')};
if(!gl){ui.flow.textContent='WebGL2 unavailable';return;}
const VS=`#version 300 es
layout(location=0) in vec3 aPos;layout(location=1) in vec3 aNormal;layout(location=2) in vec2 aUV;out vec3 n;out vec2 uv;void main(){gl_Position=vec4(aPos,1.0);n=aNormal;uv=aUV;}`;
const FS=`#version 300 es
precision highp float;in vec3 n;in vec2 uv;out vec4 c;void main(){vec3 base=vec3(.20,.48,.88);float l=.55+.45*max(dot(normalize(n),normalize(vec3(.3,.6,1.0))),0.0);c=vec4(base*l+vec3(uv*.12,0.0),1.0);}`;
function shader(t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);if(!gl.getShaderParameter(x,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(x));return x}const prog=gl.createProgram();gl.attachShader(prog,shader(gl.VERTEX_SHADER,VS));gl.attachShader(prog,shader(gl.FRAGMENT_SHADER,FS));gl.linkProgram(prog);
const pos=new Float32Array([-.65,-.55,0,.65,-.55,0,0,.65,0]);const norm=new Float32Array([0,0,1,0,0,1,0,0,1]);const uv=new Float32Array([0,0,1,0,.5,1]);
const interleaved=new Float32Array([-.65,-.55,0,0,0,1,0,0,.65,-.55,0,0,0,1,1,0,0,.65,0,0,0,1,.5,1]);
const totalBytes=interleaved.byteLength;const vboA=gl.createBuffer(),vboB=gl.createBuffer(),vaoA=gl.createVertexArray(),vaoB=gl.createVertexArray();let current='A',lastRange=null,copied=false;
function setupInterleaved(vao,vbo){gl.bindVertexArray(vao);gl.bindBuffer(gl.ARRAY_BUFFER,vbo);gl.bufferData(gl.ARRAY_BUFFER,interleaved,gl.DYNAMIC_DRAW);const stride=8*4;gl.enableVertexAttribArray(0);gl.vertexAttribPointer(0,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(1);gl.vertexAttribPointer(1,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(2);gl.vertexAttribPointer(2,2,gl.FLOAT,false,stride,24)}
function setupBatched(vao,vbo){gl.bindVertexArray(vao);gl.bindBuffer(gl.ARRAY_BUFFER,vbo);gl.bufferData(gl.ARRAY_BUFFER,totalBytes,gl.DYNAMIC_DRAW);gl.bufferSubData(gl.ARRAY_BUFFER,0,pos);gl.bufferSubData(gl.ARRAY_BUFFER,pos.byteLength,norm);gl.bufferSubData(gl.ARRAY_BUFFER,pos.byteLength+norm.byteLength,uv);gl.enableVertexAttribArray(0);gl.vertexAttribPointer(0,3,gl.FLOAT,false,12,0);gl.enableVertexAttribArray(1);gl.vertexAttribPointer(1,3,gl.FLOAT,false,12,pos.byteLength);gl.enableVertexAttribArray(2);gl.vertexAttribPointer(2,2,gl.FLOAT,false,8,pos.byteLength+norm.byteLength)}
function rebuild(){if(ui.layout.value==='interleaved'){setupInterleaved(vaoA,vboA);setupInterleaved(vaoB,vboB)}else{setupBatched(vaoA,vboA);setupBatched(vaoB,vboB)}copied=false;lastRange=null;updateUI()}
function reserve(){gl.bindBuffer(gl.ARRAY_BUFFER,vboA);gl.bufferData(gl.ARRAY_BUFFER,totalBytes,gl.DYNAMIC_DRAW);lastRange={start:0,end:0,label:'reserved only'};copied=false;updateUI()}
function write(kind){if(ui.layout.value==='interleaved'){setupInterleaved(vaoA,vboA);lastRange={start:0,end:totalBytes,label:'interleaved rebuilt'};updateUI();return}gl.bindBuffer(gl.ARRAY_BUFFER,vboA);let data,off,label;if(kind==='pos'){data=pos;off=0;label='Position'}else if(kind==='norm'){data=norm;off=pos.byteLength;label='Normal'}else{data=uv;off=pos.byteLength+norm.byteLength;label='TexCoord'}gl.bufferSubData(gl.ARRAY_BUFFER,off,data);lastRange={start:off,end:off+data.byteLength,label};updateUI()}
function copy(){gl.bindBuffer(gl.COPY_READ_BUFFER,vboA);gl.bindBuffer(gl.COPY_WRITE_BUFFER,vboB);gl.bufferData(gl.COPY_WRITE_BUFFER,totalBytes,gl.DYNAMIC_DRAW);gl.copyBufferSubData(gl.COPY_READ_BUFFER,gl.COPY_WRITE_BUFFER,0,0,totalBytes);if(ui.layout.value==='interleaved'){gl.bindVertexArray(vaoB);gl.bindBuffer(gl.ARRAY_BUFFER,vboB);const stride=32;gl.enableVertexAttribArray(0);gl.vertexAttribPointer(0,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(1);gl.vertexAttribPointer(1,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(2);gl.vertexAttribPointer(2,2,gl.FLOAT,false,stride,24)}else{gl.bindVertexArray(vaoB);gl.bindBuffer(gl.ARRAY_BUFFER,vboB);gl.enableVertexAttribArray(0);gl.vertexAttribPointer(0,3,gl.FLOAT,false,12,0);gl.enableVertexAttribArray(1);gl.vertexAttribPointer(1,3,gl.FLOAT,false,12,pos.byteLength);gl.enableVertexAttribArray(2);gl.vertexAttribPointer(2,2,gl.FLOAT,false,8,pos.byteLength+norm.byteLength)}copied=true;updateUI()}
function updateUI(){ui.memory.innerHTML='';const blocks=[];if(ui.layout.value==='interleaved'){for(let v=0;v<3;v++)blocks.push(...Array(3).fill('pos'),...Array(3).fill('norm'),...Array(2).fill('uv'))}else blocks.push(...Array(9).fill('pos'),...Array(9).fill('norm'),...Array(6).fill('uv'));blocks.forEach((k,i)=>{const d=document.createElement('div');d.className='byte '+k;const byteStart=i*4;if(lastRange&&byteStart>=lastRange.start&&byteStart<lastRange.end)d.classList.add('updated');d.textContent=i;ui.memory.appendChild(d)});ui.modeStat.textContent=ui.layout.value==='interleaved'?'INTERLEAVED':'BATCHED';ui.updateStat.textContent=lastRange?lastRange.label:'—';ui.copyStat.textContent=copied?'A → B copied':'not copied';ui.flow.textContent=ui.layout.value==='interleaved'?'VBO: [P N UV] [P N UV] [P N UV]\nstride = 8 floats\noffset(P,N,UV) = 0, 3, 6 floats':'VBO: [P P P] [N N N] [UV UV UV]\nPosition offset = 0\nNormal offset = sizeof(positions)\nTexCoord offset = sizeof(positions)+sizeof(normals)';ui.code.textContent=ui.layout.value==='interleaved'?`// Interleaved\nglBufferData(GL_ARRAY_BUFFER, sizeof(vertices), vertices, GL_STATIC_DRAW);\nglVertexAttribPointer(0, 3, GL_FLOAT, GL_FALSE, 8*sizeof(float), (void*)0);\nglVertexAttribPointer(1, 3, GL_FLOAT, GL_FALSE, 8*sizeof(float), (void*)(3*sizeof(float)));\nglVertexAttribPointer(2, 2, GL_FLOAT, GL_FALSE, 8*sizeof(float), (void*)(6*sizeof(float)));`:`// Reserve, then fill ranges\nglBufferData(GL_ARRAY_BUFFER, totalSize, NULL, GL_STATIC_DRAW);\nglBufferSubData(GL_ARRAY_BUFFER, 0, sizeof(positions), positions);\nglBufferSubData(GL_ARRAY_BUFFER, sizeof(positions), sizeof(normals), normals);\nglBufferSubData(GL_ARRAY_BUFFER, sizeof(positions)+sizeof(normals), sizeof(tex), tex);\n\n// GPU-side copy\nglBindBuffer(GL_COPY_READ_BUFFER, vbo1);\nglBindBuffer(GL_COPY_WRITE_BUFFER, vbo2);\nglCopyBufferSubData(GL_COPY_READ_BUFFER, GL_COPY_WRITE_BUFFER, 0, 0, totalSize);`;}
function draw(){const d=Math.min(devicePixelRatio||1,2),w=Math.floor(canvas.clientWidth*d),h=Math.floor(canvas.clientHeight*d);if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}gl.viewport(0,0,canvas.width,canvas.height);gl.clearColor(.03,.04,.055,1);gl.clear(gl.COLOR_BUFFER_BIT);gl.useProgram(prog);gl.bindVertexArray(current==='A'?vaoA:vaoB);gl.drawArrays(gl.TRIANGLES,0,3);requestAnimationFrame(draw)}
ui.layout.addEventListener('change',rebuild);ui.reserve.onclick=reserve;ui.writePos.onclick=()=>write('pos');ui.writeNormal.onclick=()=>write('norm');ui.writeUV.onclick=()=>write('uv');ui.copy.onclick=copy;ui.useA.onclick=()=>{current='A';updateUI()};ui.useB.onclick=()=>{current='B';updateUI()};rebuild();requestAnimationFrame(draw);
})();
